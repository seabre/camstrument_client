============
Installation
============

At the command line::

    $ easy_install camstrument_client

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv camstrument_client
    $ pip install camstrument_client
